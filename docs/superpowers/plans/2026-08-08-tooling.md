# Prettier + ESLint Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans (single task, inline execution). Steps use checkbox syntax.

**Goal:** Install and configure Prettier + ESLint per `docs/superpowers/specs/2026-08-08-tooling-design.md`.

**Architecture:** Config files only + one-time reformat; no source-logic changes.

**Tech Stack:** prettier + prettier-plugin-svelte; eslint 9 flat config + @eslint/js + typescript-eslint + eslint-config-prettier.

## Global Constraints

- Branch `feature/tooling` off main (no worktree). Suite 70 green, check 0 errors, build green after every commit.
- Reformat is its own `style:` commit; lint findings reported, not fixed.

### Task 1: install + configure + format

- [ ] Install dev deps; write `.prettierrc`, `.prettierignore`, `eslint.config.js` per spec; add `format`, `format:check`, `lint` scripts.
- [ ] Commit setup (`chore: add prettier and eslint`).
- [ ] Run `npm run format`; verify suite/check/build; commit (`style: format with prettier`).
- [ ] Run `npm run lint`; report findings (fix nothing).
